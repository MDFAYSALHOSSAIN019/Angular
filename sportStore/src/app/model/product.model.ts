

export class Product {

    // id: number = 0;
    // name: string = '';
    // category: string = '';
    // description: string = '';
    // price: number = 0;
    constructor(
       public id ?: number,
       public name ?: string,
       public category ?: string,
       public description ?: string,
       public price ?: number,

    ) { }

}